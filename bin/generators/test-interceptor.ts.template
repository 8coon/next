declare const JSWorks: any;


@JSWorks.Interceptor({ type: JSWorks.InterceptorType.%{INTERCEPTOR_TYPE}% })
export class %{NAME}%Interceptor {

    public intercept(args: object) {
        return new Promise(() => 1);
    }
}
