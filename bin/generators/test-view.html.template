<app-view id="%{NAME}%View" %{EXTENDS}%>

    <h2>This is %{NAME}%View.</h2>

    <view-if condition="$.color === 'green'">
        <view-then>
            Color is green
        </view-then>
        <view-else>
            Color is not green
        </view-else>
    </view-if>

</app-view>
